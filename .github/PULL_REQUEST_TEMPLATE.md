## Summary

What changed?

## Checklist

- [ ] `npm test` passes
- [ ] No real shell execution was added
- [ ] Terminal output still uses safe text rendering, not `innerHTML`
- [ ] `terminal.css` and `terminal.js` still work without a build step
